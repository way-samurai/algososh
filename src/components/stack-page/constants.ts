import { TStackElement } from "../../types/stack";
import { Stack } from "./class/stack-page";

export const MAXLENGTH: number = 4;
export const MAXSIZE: number = 8;

export const stack = new Stack<TStackElement>();